version: '3.8'

services:
  dashboard:
    build:
      context: dashboard
      dockerfile: Dockerfile
    container_name: dashbaord
    image: dashboard
    ports:
      - 4200:4200
    networks:
      - vocab_network